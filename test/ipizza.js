var assert = require("assert")

describe('ipizza', function() {
  it('generates valid reference number', function() {
    
  })
  it('has default providers', function() {
    
  })
  it('has gateway URLs for all providers both dev/production', function() {
    
  })
})
